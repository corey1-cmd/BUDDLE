# 글을 토론 씨앗으로 — 설계 (POST_TO_DEBATE.md)

작성: 2026-06-12 · 마이그레이션 변경 없음(기존 tags·argument_units 재사용)

## 무엇을, 왜

지금까지: 글에 **이미 화제(태그)가 있으면** 그 화제의 토론 대시보드로 들어갈 수
있다(포스트 상세의 화제 칩). 빠진 것: 글에 태그가 없거나, 글 하나를 통째로 토론의
출발점으로 삼고 싶을 때. 이번 작업은 "이 글로 토론을 시작" 흐름을 추가한다.

공론장 관점: 화제가 중심이지만, 화제는 종종 **한 사람의 글에서 시작**된다. 그
글을 토론 씨앗으로 승격(promote)하면, 그 순간 그 글의 주장·근거가 토론 대시보드의
첫 대화 축이 된다.

## 핵심 — 새 테이블 없음, 기존 파이프라인 재사용

글이 게시될 때 이미 `extract_and_store`가 그 글의 태그별로 argument_units를
추출·저장한다(0020). 따라서 "토론 씨앗 만들기"는:

1. 화제 이름을 받아 **태그 get-or-create**(이미 있는 `_attach_tags` 로직 재사용).
2. 그 태그를 글에 연결(이미 연결돼 있으면 건너뜀 — 멱등).
3. **그 태그 아래로 이 글의 argument_units를 추출**(extract_and_store) → 토론
   대시보드에 즉시 첫 축으로 등장.
4. 태그 {id, name} 반환 → 프론트가 대시보드로 이동.

이미 그 태그가 글에 있고 유닛도 있으면 1~3은 멱등(중복 생성 안 함). 즉 같은 글로
여러 번 눌러도 안전.

## API

```
POST /v1/posts/{post_id}/debate-topic
  body: { "topic_name": "도시 재개발" }   # 비우면 글의 첫 태그 사용, 그것도 없으면 400
  → 200 { "tag_id": "...", "topic_name": "도시 재개발", "seeded_units": 3 }
  → 404 글 없음/비공개   → 403 (비공개·억제 글은 토론 씨앗 불가)
```

규칙:
- **공개·비억제 글만** 토론 씨앗이 될 수 있다(비공개 생각을 공론장에 끌어내지 않음).
- 본인 글이 아니어도 토론은 시작 가능(공론장은 누구나 화제를 키울 수 있음) — 단
  태그를 **추가로 붙이는 것**이므로 원글 내용은 불변, 작성자 권리 침해 없음.
- topic_name 미입력 시: 글의 기존 첫 태그를 씨앗으로. 태그도 없으면 400(화제명을
  달라고).

## 멱등성·안전
- 태그·연결·유닛 모두 "있으면 재사용". `extract_and_store`는 같은 (post,tag)에 대해
  유닛을 다시 만들지만, 대시보드 집계는 텍스트 기준이라 표시상 중복은 무해(향후
  중복 억제 최적화 여지). 우선은 단순·정확 우선.
- leukocyte: 글은 게시 시 이미 검열됨. 토론 씨앗은 새 콘텐츠를 만들지 않으므로 추가
  검열 불필요.

## 프론트
포스트 상세에서:
- 글에 태그가 있으면: 기존 화제 칩 + "새 화제로 토론 시작" 옵션.
- 글에 태그가 없으면: "이 글로 토론 시작하기" 단일 진입 — 화제명을 입력(또는 글
  제목/첫 문장 제안)받아 승격 후 대시보드로.

## 구현 순서
1. 서비스 `argument_service.promote_post_to_topic` (멱등, 기존 헬퍼 재사용).
2. API `POST /v1/posts/{post_id}/debate-topic` + 스키마.
3. 프론트: 포스트 상세에 태그 유무별 진입 + 화제명 입력 시트.
4. 테스트: 멱등·공개검사·씨앗 유닛 생성·태그 재사용.
게이트: pytest 전량 / ruff / mypy --strict.
