# buddle 종합 검증 보고서

검증 일자: 2026-05-18 · 대상: buddle 백엔드 (5-AI + EKB 인지 + 광장 + 피드백 루프)
정적 품질: ruff 0 이슈 · mypy(strict) 134 소스 0 이슈 · 전체 테스트 220 passed / 62 skipped(DB 필요, CI 실행)

각 검증 항목을 **3개의 독립 루트**로 교차 검증했다. 한 루트가 놓친 결함을 다른 루트가 잡도록 설계했고, 실제로 검증 과정에서 결함 2건을 발견·수정했다.

---

## 검증 1 — 코드가 목적에 맞게 작동하는가 (기능 정확성)

| 루트 | 방법 | 결과 |
|---|---|---|
| 1-A | pytest 전체 스위트 실행 | **220/220 통과** (DB 62 skip) |
| 1-B | pytest 무관 독립 동작 시뮬레이션 (실제 호출 경로 16종) | **16/16 동작 정상** (단언 1건 정정) |
| 1-C | EKB 이론 기대값 대조 (수렴·포화·의도→전략) | **6/6 일치** |

**발견·수정 1**: 1-A에서 `test_persona_backends`의 대화 히스토리 테스트 1건 실패. Part 1/3에서 인지 블록(system 메시지)을 프롬프트에 주입하도록 바꾼 것이 메시지 개수를 늘린 정상 회귀였음 → 테스트를 현재 설계(cognition 블록 포함)에 맞게 갱신.

근거: 의도별 응답 전략(합격→CELEBRATE, 우울→COMFORT, 질문→INFORM, 요청→ASSIST), 백혈구 자해→COMFORT+109, 위해→거부, 매개자 편향→균형, 윤리 다중라벨+자소 우회 탐지 전부 정상.

---

## 검증 2 — 가중치·토큰 관계가 에러·변수 없이 의도대로

| 루트 | 방법 | 결과 |
|---|---|---|
| 2-A | 경계값 + 극단 케이스 + 가중치 합=1 불변식 | 경계/극단 **10/10**, 정규화 **1000/1000** |
| 2-B | 속성 기반 fuzzing 8000건 (불변식 위반 카운트) | **위반 0건** (cognition/importance/affinity/dispositions) |
| 2-C | 수렴성 + 민감도 + 피드백 가드레일 불변식 | 수렴·민감 **6/6**, 피드백 **6/6** |

검증한 불변식:
- 중요도 `normalize`: 모든 raw(±1e7)에서 [−i_max, +i_max], tanh 단조 포화 (raw→∞ 시 정확히 i_max로 수렴, 초과 없음)
- 친화도 `apply_affinity`: 100회 누적해도 [−0.3, +0.3] 클램프, 정확히 캡에서 포화
- proactivity offset: 모든 입력에서 [0, 1], warmth/안전 불변
- 매개자 가중치 α+β+γ=1, 자동조정 step ≤ 0.05, 게이트 미만 무시, 신고 비대칭(좋아요보다 빠른 하강)
- 의사결정 후보 점수: 유한·[0, 5] 범위

fuzzing 입력 종류: 한국어/영어/혼합/빈문자열/초대형(800단어)/문장부호/유니코드+이모지.

---

## 검증 3 — 각 코드가 서로 잘 도는가 (통합)

| 루트 | 방법 | 결과 |
|---|---|---|
| 3-A | 전체 모듈 import + 라우트 그래프 | **133 모듈 import 성공/0 실패**, v1 55 라우트 정상 |
| 3-B | 모듈 간 협력 end-to-end (4개 흐름) | **7/7 통과** |
| 3-C | mypy(strict) 타입 계약 정적 검증 | **134 소스 0 이슈** |

검증한 협력 흐름:
- 대화 프롬프트 체인: affect → cognition(정보처리→백혈구→매개자→의사결정→synthesize) → prompts → 백엔드 메시지. 자해 신호 시 안전 가이드(109) 주입 확인.
- 윤리 흐름: 다중라벨 분류 → 레거시 severity 후방호환 집계 → leukocyte 카테고리별 차단 판정.
- 피드백 흐름: 피드 통계 → 친화도 누적 → proactivity offset → 인지 반영(전략 클래스 불변 = 안전 유지).
- 광장 흐름: cadence 읽음 판정 → 댓글 트리거 수 일관성 + 상한 준수.

---

## 검증 4 — 보안 검사

| 루트 | 방법 | 결과 |
|---|---|---|
| 4-A | 정적 보안 패턴 스캔 (위험함수/SQL조합/하드코딩/평문) + ruff-bandit | 실제 위험 **0건** (bandit 6건 전수 안전 오탐) |
| 4-B | 의존성 취약점 스캔 (pip-audit) | buddle 직접 의존성 취약점 **1건 발견 → 수정** |
| 4-C | 보안 단위 테스트 (H1/H2/M1-M4/L1-L2) | **20/20 통과** |

**발견·수정 2 (중요)**: 4-B에서 `pyjwt 2.7.0` 취약점 2건(PYSEC-2026-120, PYSEC-2025-183) 발견. 환경 복원 시 데비안 시스템 패키지로 깔린 구버전이 원인(pyproject는 `>=2.10.0` 요구). `pyjwt 2.13.0`으로 강제 업그레이드 → 취약점 해소, JWT 동작 회귀 20건 통과 확인.

4-A bandit 6건 분석(전부 안전 오탐):
- `_DEV_JWT_SECRET`(S105): production validator가 起動 거부하는 dev sentinel
- `0.0.0.0`(S104): 컨테이너 표준 바인딩
- `token_type`="access"/"refresh"(S105/S106 ×4): 토큰 종류 문자열, 비밀번호 아님

정적 스캔: SQL 문자열 조합 0, 하드코딩 실값 시크릿 0, 평문 비밀번호 로깅 0, 위험 `eval`/`exec`/`pickle`/`shell=True` 0 (검출된 `model.eval()`=PyTorch, `redis.eval()`=Lua는 안전).

4-C 보안 테스트 항목: 프로덕션 시크릿 거부(dev기본/동일키/32자미만), 패스워드 복잡도, 로컬 백업 리미터, 리프레시 토큰 회전+재사용탐지+사용자불일치, 계정 잠금, SSRF(스킴/메타데이터/loopback/사설차단/opt-in/메타데이터-항상차단).

---

## 제출물 요약 — 테스트 결과 (이름 / 내용 / %)

### 파일별 단위·검증 테스트 (실제 실행, DB 불필요)

| 테스트 파일 | 검증 내용 | 통과율 |
|---|---|---|
| test_cognition_information.py | EKB 정보처리 5단계(노출~보유)·의도분류·편향계약 | 21/21 (100%) |
| test_cognition_decision.py | EKB 의사결정(문제인식~선택)·페르소나 가중·안전오버라이드 | 17/17 (100%) |
| test_cognition_safety.py | 백혈구 양심게이트·매개자 편향해소 | 18/18 (100%) |
| test_ethics_taxonomy.py | MLCommons 13카테고리·다중라벨·0-7·자소·후방호환 | 16/16 (100%) |
| test_ethics.py | 윤리 분류 severity·집계 | 6/6 (100%) |
| test_feedback_loop.py | 피드백 게이트·step·클램프·비대칭·인지반영 | 15/15 (100%) |
| test_plaza.py | 에이전트 키 crypto·스키마·enum | 10/10 (100%) |
| test_plaza_virtual.py | 가상페르소나 cadence·읽음판정·역할프롬프트 | 15/15 (100%) |
| test_affect.py | 언어 정서 인지·편향계약 | 12/12 (100%) |
| test_security_hardening.py | H1/H2/M1-M4/L1 보안 | 20/20 (100%) |
| test_importance.py | 중요도 tanh·분배배수·피드점수 | 11/11 (100%) |
| test_technician_core.py | HMAC 해시체인·권한상태 | 16/16 (100%) |
| test_central_core.py | 건강판정·자동조정 가드레일 | 12/12 (100%) |
| test_central_stage6.py | 중앙관리자 리포트 | 10/10 (100%) |
| test_analysis_suite.py (verification) | 수렴·민감도·경계·극단·FEM분해 | 22/22 (100%) |
| 기타(auth/personas/embeddings/mediator 등) | 단위 | 100% |

### 교차 검증 루트 (이 보고서)

| 검증 | 루트 A | 루트 B | 루트 C |
|---|---|---|---|
| 1 기능정확성 | 220/220 | 16/16 | 6/6 |
| 2 수치무결성 | 1010/1010 | 8000/8000 (위반0) | 12/12 |
| 3 통합 | 133모듈/55라우트 | 7/7 | 134소스 0이슈 |
| 4 보안 | 위험0(오탐6) | 취약점1→수정 | 20/20 |

**종합: 전체 테스트 220 passed / 62 skipped(DB), 정적 0이슈, 교차검증 전 루트 통과, 발견 결함 2건 전부 수정.**

---

## 개선 방안

### 즉시 (운영 전 권장)
1. **DB 통합 테스트 실행**: 현재 62개가 Docker 부재로 skip. CI(`.github/workflows/ci.yml`)에 pgvector+Redis로 정의돼 있으나, 로컬/스테이징에서 `alembic upgrade head` + 전체 통합 테스트 1회 완주 필요. (마이그레이션 0001~0009 실DB 검증 포함)
2. **의존성 핀 강화**: 환경 복원 시 데비안 시스템 pyjwt가 우선되는 문제 → `uv`/가상환경 격리로 pyproject 버전이 항상 적용되게. CI에 `pip-audit` 게이트 추가(직접 의존성 취약점 시 빌드 실패).

### 단기 (품질 향상)
3. **윤리 분류기 실모델화**: 현재 stub은 규칙 기반(데모/테스트용). 조사한 대로 local_hf에 KR-BERT/Llama Guard 3를 K-MHaS/KOLD/K-HATERS로 파인튜닝해 연결. 같은 `EthicsClassifier` 프로토콜이라 교체만 하면 됨. 경계선 합성 데이터로 false positive 최소화.
4. **좋아요 전용 테이블**: 피드백 루프가 현재 "분배 수"를 좋아요 프록시로 사용. 명시적 like 테이블 도입 시 신호 정밀도 향상.
5. **bandit 오탐 억제 주석**: S105/S104/S106 6건에 `# noqa: S10x` + 사유 주석을 달아 보안 스캐너 CI를 깨끗하게(현재 평소 ruff엔 미포함이라 무해하나, 보안 게이트 도입 시 필요).

### 중기 (견고성·관측성)
6. **WebSocket·실시간 경로 통합 테스트**: dialogue WS 첫프레임 인증, plaza tick의 게시·댓글 트리거 전체 흐름을 DB 띄운 환경에서 E2E 검증(현재 순수 로직만 검증됨).
7. **부하·동시성 테스트**: rate limiter(Redis Lua + 로컬 백업), 권한 advisory lock, family rotation 경합을 동시 요청으로 검증.
8. **관측성**: 보안 이벤트(로그인실패·토큰재사용·SSRF차단)·윤리 차단·피드백 조정에 메트릭/대시보드 연결(이미 prometheus-client·security_events 기반 존재).

### 장기 (보안 심화)
9. **적대적 입력/탈옥 테스트**: 윤리·백혈구 게이트에 대한 jailbreak·우회 프롬프트 레드팀(조사한 "Guardians and Offenders" 분류 활용).
10. **응답 측 검사 강화**: 현재 입력(게시) 측 검사가 주력. Llama Guard 문헌상 응답 분류가 적대적 공격에 더 강건하므로, 페르소나 출력에도 윤리 분류를 적용하는 2차 게이트 추가 검토.
